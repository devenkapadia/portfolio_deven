import { makeStyles } from '@material-ui/core';

export const useStyles = makeStyles((theme) => ({
  appBar: {
    background: '#222',
  },
  clearAll: {
    background: 'tomato',
  },
  title: {
    color: 'tan',
    fontWeight:"bold",
    fontSize: '1.4rem',
    textDecoration: 'none',
  },
}));
